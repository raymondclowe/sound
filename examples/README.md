# Examples

This folder contains example WAVs that can be used to build reference profiles for the demo or tests.

Use the script `scripts/generate_example_wavs.py` to generate example WAV files that are short tones.

Example files:
- example_computer_male.wav
- example_computer_female.wav
- example_computer_male_teen.wav
